/** localStorage key for the remembered desktop sidebar collapse state. */
export const SIDEBAR_STORAGE_KEY = "garp-portal:sidebar"

/**
 * Key that toggles the rail, combined with Cmd (macOS) or Ctrl (elsewhere).
 * Compared against `KeyboardEvent.key`, so keep it lowercase.
 */
export const SIDEBAR_TOGGLE_KEY = "b"
