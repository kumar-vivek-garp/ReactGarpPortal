import bgDarkGradient from "@/assets/brand/bg-dark-gradient.jpg"

/**
 * The GARP wordmarks are no longer here — they are inline SVG components
 * (`atoms/garp-logo-mark`, `atoms/garp-logo-full`) so their colour can follow
 * the surface they sit on. The raster copies under `assets/brand/` are now
 * unreferenced.
 */

/** Auth / pending shell background. */
export const GARP_AUTH_BG = bgDarkGradient
